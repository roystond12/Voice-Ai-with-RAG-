# environments
.venv/
.env
frontend/.env.local

# python
__pycache__/
*.pyc

# vector db storage (regenerated locally / by the vector store server)
qdrant_db/
qdrant_db1/
chroma_db/
chroma.log

# ONNX model exports (regenerate with conversion/ scripts — large binaries)
onnx_generation_output/
onnx_reranker_output/

# node / frontend
frontend/node_modules/
frontend/dist/

# streamlit (legacy, no longer used)
.streamlit/
